# floorp CSS

Custom `userChrome.css` / `userContent.css`. Not stowed

```bash
PROFILE=~/.var/app/one.ablaze.floorp/.floorp/*.custom
mkdir -p $PROFILE/chrome
cp userChrome.css userContent.css $PROFILE/chrome/
```
